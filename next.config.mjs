/** @type {import('next').NextConfig} */
const nextConfig = {
  // Static export
  // basePath: "/itsmushfik",
  images: {
    unoptimized: true,
  },
  output: "export",
  reactStrictMode: true,
};

export default nextConfig;
